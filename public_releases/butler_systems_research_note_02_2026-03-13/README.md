# Butler Systems Research Note 02

## Central Activation and Exact Collapse in a Rotor Closure

Date: 2026-03-13

Canopy: `Butler Systems`

Release class: public note

## Abstract

This note fixes the exact central collapse lane for the rotor closure. Starting from the closure/projection architecture carried by the earlier hinge-seam spine, it records the central activation map

`phi : A4 x Z2(Delta) ->> C3 x Z2(Delta)`

with kernel

`ker(phi) = V4 x {e}`.

This gives an exact `12 -> 6` collapse in the central activation lane and makes precise the separation between configuration closure and operational projection.

The claim is intentionally narrow: this note treats only the exact central collapse. It does not publish the twisted/noncentral activation classification, the `Delta`-event dynamics, or broader universality claims.

## Exact Public Claim

This note publicly claims only:

1. the exact central collapse map
2. the kernel `V4 x {e}`
3. the two-level read:
   - `12 = closure`
   - `6 = operational projection`
4. the fact that the operational six-layer is not being presented here as an embedded subgroup of the closure layer

## Explicit Boundary

This note does **not** claim:

1. the noncentral `Delta` classification package
2. the dynamical `Delta`-event bridge
3. witness-domain confirmation of the full collapse package
4. broad universality
5. the full Butler Systems ontology as a novelty claim

## Positioning

This is the second Butler Systems novelty egg.

It fixes the exact central collapse before the twisted classification and event-identification layers are released.
