# Butler Systems Research Note 03

## Central and Noncentral Activation Lanes in a Rotor Closure

Date: 2026-03-13

Canopy: `Butler Systems`

Release class: public note

## Abstract

This note classifies the operational six-state algebra of the rotor closure under central and noncentral `Delta` activation. For

`G_rho = A4 ⋊_rho Z2(Delta)`

with

`K = V4 x {e}`,

the collapsed operational quotient is exactly one of:

- `C6`
- `S3`

with branch selector determined by the induced `Delta` action on `A4 / V4 ~= C3`.

The claim is intentionally narrow: this note classifies the exact quotient types in the central and noncentral lanes. It does not claim that the underlying physical or dynamical mechanism has already selected the branch, and it does not publish the `Delta`-event bridge.

## Exact Public Claim

This note publicly claims only:

1. the quotient classification for `G_rho`
2. the central lane quotient type
3. the noncentral lane quotient type
4. the branch selector on the induced quotient action
5. the finite activation class register `C0 / C1 / C2` as a classification object

## Explicit Boundary

This note does **not** claim:

1. that the actual rotor dynamics have already fixed the branch
2. the full `Delta`-event dynamical bridge
3. witness-domain confirmation of the whole classification package
4. global crossing-spectrum or renormalization claims
5. the full Butler Systems ontology as a novelty claim

## Positioning

This is the third Butler Systems novelty egg.

It extends the exact rotor closure into the central/noncentral activation classification while leaving the event-identification problem explicitly open.
